# 边狱巴士都市规则 · FVTT 系统开发总览

**Foundry VTT v13+ 自定义游戏系统**
系统 ID：`limbusCompany_FVTT` | 当前版本：`0.1.0` | 更新：2026-08-07

> 本文件是项目现状的索引与总览。核心系统（角色卡、全部 8 种物品类型、对抗引擎、BUFF 系统、背景创建向导、升级流程、以及全部独立 HUD/工具应用）均已实现并可用——这不再是一份"待办路线图"，而是"现在有什么、在哪里"的地图。规则细节以 `策划文件/` 为准，代码架构与开发流程见 `CLAUDE.md`。

---

## 目录

1. [项目结构总览](#一项目结构总览)
2. [策划文件索引](#二策划文件索引)
3. [素材资源索引](#三素材资源索引)
4. [已实现功能总览](#四已实现功能总览)
5. [核心数据模型速查](#五核心数据模型速查)
6. [技术关键点清单](#六技术关键点清单)
7. [已知空白与后续方向](#七已知空白与后续方向)

---

## 一、项目结构总览

```
limbusCompany_FVTT/
├── 开发总览.md               ← 本文件（总纲，开发入口）
├── CLAUDE.md                 ← 架构与开发流程指南（AI开发指南，规则细节见策划文件/）
├── system.json                ← 系统清单：Actor/Item 类型注册、版本、语言
├── template.json              ← 遗留兼容占位（真实 schema 见 module/documents/）
├── lang/zh-CN.json            ← 中文本地化字符串
├── 策划文件/                 ← 所有策划设计文档（详见第二节）
├── assets/icons/               ← 图标/GUI 参考素材（详见第三节）
├── module/
│   ├── limbusCompany_FVTT.mjs  ← 系统入口：init/setup/ready 钩子、socket 分发、模板预加载
│   ├── config.mjs              ← 全局常量（挂载到 CONFIG.LIMBUSCOMPANY）
│   ├── documents/
│   │   ├── actor.mjs           ← LimbusActor + CharacterData/MerchantData/CampData/LootData
│   │   └── item.mjs            ← LimbusItem + 8 种 Item 的 TypeDataModel
│   ├── sheets/
│   │   ├── actor-sheet.mjs     ← 角色卡（最大的 Sheet，物品/技能/战斗三 Tab）
│   │   ├── item-sheet.mjs      ← 全部 8 种物品共用一个 Sheet 类，模板分流
│   │   ├── merchant-sheet.mjs / camp-sheet.mjs / loot-sheet.mjs
│   │   ├── background-wizard.mjs  ← 4 步角色创建向导
│   │   ├── level-up-dialog.mjs    ← 2 步升级对话框
│   │   ├── gm-console.mjs / squad-hud.mjs / quick-action-hud.mjs
│   └── helpers/
│       ├── clash.mjs           ← 对抗/拼点引擎（全仓库最大文件，约 3700 行）
│       ├── custom-buffs.mjs    ← BUFF 扩展注册表
│       ├── dice.mjs            ← 属性鉴定（硬币）弹窗
│       ├── bag-grid.mjs        ← 背包物品打包为网格显示的公共算法
│       ├── item-piles.mjs      ← Item Piles 模组联动注册
│       └── sin-resource-hud.mjs← 全局七宗罪资源池 HUD
├── templates/                  ← 全部 .hbs 模板（详见第四节各功能对应关系）
└── styles/limbusCompany_FVTT.css
```

---

## 二、策划文件索引

> 遇到任何功能疑问，**直接查阅对应策划文件**，策划文件是最终规则裁决依据；代码是这些文档的落地实现。

| 文件 | 内容 | 关键章节 |
|------|------|---------|
| `策划文件/大纲.txt` | 系统整体规则大纲（原始版本），包含角色卡/物品/技能/状态/对战/背景全貌 | 全文 |
| `策划文件/角色卡设计.txt` | 角色卡 HBS/CSS/JS 制作规格，含颜色值、布局尺寸、交互说明 | §1-底部 |
| `策划文件/角色卡设计文档.md` | 角色卡精细规格文档 v1.4，包含三大 Tab 页（物品/技能/战斗）完整规格 | §3区域C-E, §11技能页, §12战斗页 |
| `策划文件/对抗.md` | 对抗系统完整流程：发起→聊天框→拼点→承受，含所有 UI 弹窗规格 | §3-§9 |
| `策划文件/物品卡设计.md` | 物品卡通用设计概述（简版） | 全文 |
| `策划文件/物品卡-装备.md` | 装备物品卡详细规格，含效果触发编辑器字段说明 | §6效果触发编辑器 |
| `策划文件/物品卡-技能.md` | 技能物品卡（基础/守备/EGO）详细规格，含BUFF下拉框选项 | §4-§9 |
| `策划文件/物品卡-材料消耗品.md` | 材料和消耗品物品卡详细规格 | §2-§3 |
| `策划文件/物品卡-容器.md` | 容器物品卡规格（网格存储） | §2-§3 |
| `CLAUDE.md` | 代码架构、常用模式（编辑锁/Title卡/物品引用/socket委托更新等）、开发流程（AI 开发标准参考） | 全文 |

背景系统（背景物品类型、创建向导、升级奖励）目前没有独立的策划文件，其规格以代码实现为准，见第四节。

---

## 三、素材资源索引

### Base_icon（基础图标）

| 文件 | 用途 |
|------|------|
| `Slash.webp / Blunt.webp / Pierce.webp` | 物理伤害类型：斩击/打击/突刺 |
| `Offense_Level.webp / Defense_Level.webp` | 攻击等级/防御等级图标 |
| `Health.webp / Sanity.webp / Speed.webp / Starlight.webp` | 生命/理智/速度/星芒图标 |
| `Wrath/Lust/Sloth/Gluttony/Gloom/Pride/Envy_icon.webp` | 七宗罪资源图标 |
| `硬币_正面.webp / 硬币_反面.webp` | 鉴定用硬币图标（已接入 DiceSoNice 自定义 d2 面） |
| `闪避/防御/反击/可拼点防御.webp` | 守备技能类型图标 |
| `不可摧毁的硬币/截除硬币.webp` | 特殊骰类图标（`DICE_TYPES` 已定义 unbreakable/severing，UI 尚未全面启用，见第七节） |
| `眼.webp` | 货币（眼）图标 |
| `重置.svg / Link.svg` | 重置按钮/链接图标 |

### Buff_icon（状态图标）

包含全部 BUFF/DEBUFF 图标：强壮/虚弱、忍耐/破绽、迅捷/束缚、守护/易损、拼点威力提升/降低、攻击等级提升/降低、防御等级提升/降低、烧伤、流血、震颤（+叠加+引爆）、破裂、沉沦、呼吸法、充能、陷入混乱、陷入恐慌、麻痹、广域乱射、弹药、护卫、振幅转换。

### Skill（技能图标）

- `Normalsin.webp`：空槽位占位图
- `{Sin}_lv{1-3}.webp`：七宗罪×三等级技能图标，共21张
- `E.G.O.webp`：EGO技能图标

### GUI（界面原型参考截图）

| 文件 | 开发用途 |
|------|---------|
| `角色卡-装备页面.png` | 角色卡物品 Tab 整体布局参考 |
| `角色卡-技能页面.png` | 角色卡技能 Tab 布局参考 |
| `角色卡-战斗页面.png` | 角色卡战斗 Tab 布局参考 |
| `物品卡-技能界面.png` | 技能物品卡锁定状态参考 |
| `物品卡-技能-相关技能扩展.png` | 相关技能展开状态参考 |
| `物品卡-技能-解锁编辑界面.png` | 技能物品卡编辑状态参考 |
| `物品卡-装备界面.png` | 装备物品卡参考 |
| `物品卡-装备-解锁编辑界面.png` | 装备编辑状态参考 |
| `物品卡-材料消耗品.png` | 材料/消耗品物品卡参考 |
| `物品卡-材料消耗品-解锁编辑界面.png` | 材料/消耗品编辑参考 |
| `物品卡-容器.png` | 容器物品卡参考 |
| `物品卡-效果触发编辑器.png` | Activity 编辑器 UI 参考 |
| `物品页面-过滤.png` | 过滤面板 UI 参考 |
| `发起对抗玩家-A.png` | 对抗发起聊天框参考 |
| `进行对抗-玩家B.png` | 对抗响应聊天框参考 |
| `处理对抗结果聊天框.png` | 拼点结算聊天框参考 |
| `鉴定聊天框.png` | 鉴定弹窗+结果聊天框参考 |
| `添加新的状态BUFF.png` | 添加BUFF弹窗参考 |
| `下拉框.png` | 下拉框通用样式参考 |
| `Title-技能.png / Title-物品.png` | 悬停 Title 卡片参考 |

---

## 四、已实现功能总览

### Actor 类型（4 种，均已实现）

| 类型 | Sheet 类 | 模板 | 核心功能 |
|------|---------|------|---------|
| `character` | `LimbusActorSheet`（`module/sheets/actor-sheet.mjs`，最大的 Sheet） | `templates/actor/character-sheet.hbs` + `parts/{header,tab-items,tab-skills,tab-combat}.hbs` | 三栏头部（头像/HP/理智 \| 基础信息/抗性 \| 姓名/属性）；物品 Tab（3×3 装备格 + 物品列表 + 过滤面板 + 悬停 Title 卡）；技能 Tab（技能槽 + 技能列表）；战斗 Tab（BUFF 状态栏 + 6-bag 技能区 + AP 硬币）；固定底栏（"眼"货币 + 星芒） |
| `merchant` | `LimbusMerchantSheet` | `templates/actor/merchant-sheet.hbs` | 商店描述 + 商人货币；通过 Item Piles 联动实现买卖（物品的 price/stock/hidden 字段） |
| `camp` | `LimbusCampSheet`（第二大 Sheet） | `templates/actor/camp-sheet.hbs` | 仓库网格（默认 7×7）+ 制作配方系统（材料列表 + 产出物品 + 隐藏配方 GM 拖入产出物） |
| `loot` | `LimbusLootSheet` | `templates/actor/loot-sheet.hbs` | 战利品网格（默认 5×5）+ `revealed` 揭示机制（未揭示物品显示剪影且不可拾取）+ GM 加权战利品表补货 |

四类 Sheet 均实现 `handleSocketMsg`，跨客户端更新经由共享 socket 通道委托 GM 执行（见 `CLAUDE.md` 的 `_safeDocUpdate` 说明），不止对抗系统用到这个模式。

### Item 类型（8 种，均已实现）

统一挂在单个 `LimbusItemSheet`（`module/sheets/item-sheet.mjs`）上，按 `item.type` 分流到对应模板；`material` 与 `consumable` 共用同一模板（外观相同，仅字段/标签不同，无独立 `material-sheet.hbs`）。

| 类型 | 模板 | 关键字段/机制 |
|------|------|---------|
| `equipment` | `equipment-sheet.hbs` | subtype（上装/下装/武器/饰品）、抗性调整（仅上装）、atk/def/speed 调整、stellarCost、Activity 效果触发、商人字段 |
| `skill` | `skill-sheet.hbs` | type（basic/defense/ego）、Lv1-3、罪孽类型、EGO 等级(ZAYIN~ALEPH)与对应星芒消耗（自动派生）、骰子公式自动计算、`relatedSkill`（相关技能池 + 侵蚀形态 UUID）、多罪孽 EGO 消耗 |
| `consumable` / `material` | `consumable-sheet.hbs`（共用） | 数量、可重用/无限标记、Activity（仅消耗品）、商人字段 |
| `container` | `container-sheet.hbs` | 自定义网格尺寸（默认 3×3）、`contents[]` 定位存放、锁定格 |
| `skillbook` | `skillbook-sheet.hbs` | 内含技能列表（uuid 或快照）、`learnAllSkills()` 一键学习全部技能并消耗技能书 |
| `panic` | `panic-sheet.hbs` | 描述 + Activity（触发时机"恐慌触发时"/"坚定触发时"），供 `CharacterData.panicSlots` 使用 |
| `background` | `background-sheet.hbs` | 简介、初始物品、按等级解锁的升级奖励物品、`category`（供创建向导按合集筛选） |

`LimbusItem` 文档类另实现：`getStellarCost()`、骰子公式计算与投骰、相关技能池解析、Activity 增删改、收藏切换、`sendToChat()` 聊天卡片渲染。

### 对抗/拼点引擎 — `module/helpers/clash.mjs`（约 3700 行，全仓库最大文件）

单一静态类 `ClashManager`，覆盖完整流程"发起对抗 → 聊天框 → 进行对抗确认 → 拼点结算 → 承受"，已实现能力包括：

- 骰值/效果强度公式解析（支持 `1D6+2` 形式）
- **跨权限安全更新**（`_safeDocUpdate`）：任意客户端都能通过 socket 委托 GM 触发对任意角色的效果
- **Activity 触发/结算引擎**（`_applyActivities` 系列，约 600 行）：驱动全部物品的效果触发（命中时/回合开始/受伤时等 16 种时机 × 10 种效果类型，定义见 `config.mjs` 的 `ACTIVITY_TRIGGERS`/`ACTIVITY_EFFECTS`）
- 拼点发起/响应/结算对话框（`showInitiateDialog` / `showPerformDialog`，约 700 行）：硬币投掷、抗性倍率、伤害计算
- 流血状态处理、EGO 抗性调整、重投（反应触发）
- 直接承受（无拼点）流程、加重扩散伤害机制、不可摧毁反击机制
- **反应系统**（约 250 行）：物品/技能可注册"反应"触发时机，对其他角色的行动做前置条件判定后插入效果
- 统一 socket 分发入口 `handleSocketMsg`（`clashResolve`/`activityActivate`/`reactionCheck`）

### BUFF/状态系统 — `module/helpers/custom-buffs.mjs`

`CustomBuffRegistry` + `registerCustomBuff(type, handler)` 扩展点（钩子：`onRoundEnd`/`modifySpeedRoll`/`onClashWin`/`beforeChaos`/`modifyResistances`/`onTakeDamage`）。已注册的具体机制型 BUFF：

- `defensiveStance`（防御姿态）：最多2层、获得时刷新、回合结束衰减、强制最低速度、拼点获胜消耗对方震颤触发震颤引爆、混乱免疫
- `butterfly`（蝶）：受伤时消耗1层，为攻击方回复1d6理智，自身获得沉沦
- `piercingArrow`（刺入之矢）：强制斩击抗性为 x2.0
- `bloodFlame`（血炎）：最多3层、获得时刷新、回合结束衰减、仅对"烧伤"来源的混乱免疫

其余（震颤/震颤引爆/沉沦/烧伤/破裂/流血/充能/呼吸法/弹药/护卫）仅注册标签/描述文本，具体数值效果由 `clash.mjs` 通用 BUFF 强度/层数模型处理，或作为 Activity 效果直接实现。

### 角色创建与升级

- **`background-wizard.mjs`**（4 步向导）：① 背景选择（世界物品+合集包浏览器，按 `category`/所在文件夹筛选）→ ② 属性点分配（预算=六项初始值之和，单项范围 2–8）→ ③ 恐慌卡选择（士气低落/陷入恐慌分别选择）→ ④ 初始物品确认，完成后写入角色（背景引用 + 属性 + 恐慌卡嵌入 + 初始物品发放）
- **`level-up-dialog.mjs`**（2 步对话框）：步骤1展示等级/生命值/星芒的"原→现"，点击"下一步"即调用 `LimbusActor.levelUpByXp()` 真正应用升级并按背景 `levelRewards` 发放本级奖励物品；步骤2展示已发放的奖励物品，点击"完成"关闭

### 独立 HUD / 工具应用

| 应用 | 文件 | 触发方式 | 功能 |
|------|------|---------|------|
| GM 控制台 | `gm-console.mjs` | 快捷键 `M`（仅GM） | 批量长休、批量发放经验值给拖入的角色名单 |
| 小队 HUD | `squad-hud.mjs` | 快捷键 `Z`/`X`（队伍1/2，仅GM） | 显示指定角色的 HP/理智/AP/BUFF 图标，成员配置存世界设置 |
| 快捷操作面板 | `quick-action-hud.mjs` | 选中单个角色 Token 时自动出现 | 头像+HP/理智球、AP硬币、可展开的消耗品/装备/基础技能/EGO技能面板 |
| 全局罪孽资源 HUD | `sin-resource-hud.mjs` | `ready` 钩子自动创建，`globalThis.SinResourceHUD` 供宏调用 | 世界共享的七宗罪资源池，GM直接编辑/玩家经 socket 委托 |
| 属性鉴定弹窗 | `dice.mjs`（`AttributeCheckDialog`） | 点击角色卡属性值 | 难度可选（最多8枚硬币），生成聊天结果卡 |
| 背包网格打包 | `bag-grid.mjs` | 内部工具函数 | 角色卡物品Tab与营地界面共用的首次适应网格打包算法 |
| Item Piles 联动 | `item-piles.mjs` | `item-piles-ready` 钩子 | 向 Item Piles 模组注册本系统的战利品堆/商人/仓库配置 |

---

## 五、核心数据模型速查

### Actor 数据（character 类型）

| 字段 | 公式/默认值 | 说明 |
|------|-----------|------|
| `hp.value/max` | `等级d10 + 体质×5` | 生命值 |
| `sanity.value` | 5–95，默认 50 | 理智值，≤5 触发恐慌 |
| `speed` | `1D6 + 敏捷`（范围展示） | 先攻/速度骰 |
| `ap.value/max` | 固定上限 3 | 行动点，每回合结束重置 |
| `stellarMotes.max` | `30 + 等级` | 星芒上限 |
| `attrPoints` | 每10级+1 | 属性点（升级时按等级发放） |
| `chaosThresholds` | 默认 `[{60,false},{30,false}]` | 混乱阈值；体质>7→1条(50%)，敏捷>7→3条(20/50/80%)；震颤引爆会永久性前移所有阈值百分比，直到长休重置 |
| `attributes.*` | 六属性，schema范围2–10（创建向导限定分配2–8） | str/agi/con/int/per/cha |
| `background.uuid` | 指向 `background` 类型物品的 UUID | 由创建向导写入 |
| `panicSlots.{lowMorale,panic}` | 嵌入角色的 `panic` 类型物品 ID | 由创建向导或角色卡右键菜单设置 |
| `sins.*` | 七宗罪资源（全局共享，见 `SinResourceHUD`） | wrath/lust/sloth/gluttony/gloom/pride/envy |

### Item 类型一览

见第四节表格；策划文档对照关系保留在第二节索引。

---

## 六、技术关键点清单

> 完整的代码模式说明（编辑锁、Title卡悬停预览、物品引用 schema、右键菜单、搜索框防抖+IME 处理、多步向导 state、socket 委托更新）见 `CLAUDE.md` ——这里只列不易从代码直接读出的关键数值/约定。

### 必须注意的实现细节

- **Schema 改动需要重启 world**：`TypeDataModel.defineSchema()` 的字段变更不会热更新，模板/CSS/普通逻辑改动可以。
- **新增 Item/Actor 类型需同步 3+ 处**：`system.json` → `documentTypes`、`module/limbusCompany_FVTT.mjs` 的 `CONFIG.Actor/Item.dataModels`、对应 Sheet 的模板类型映射。`template.json` 是遗留兼容占位，当前落后于 `system.json`（缺 `panic`/`background`），不是权威来源。
- **新模板必须加入预加载列表**：`module/limbusCompany_FVTT.mjs` 的 `_preloadTemplates()`。
- **`_registerSettings()` 目前是空占位**（"保留槽位，后续阶段按需添加"）——`globalSins`（`SinResourceHUD.init()`）与小队成员设置（`SquadHUD.init()`）是各自在 `init` 钩子里独立注册的，没有走这个统一入口。

### 关键颜色常量

沿用角色卡设计文档规定的配色（见 `styles/limbusCompany_FVTT.css` 与各 `策划文件/` 对应色值表）。

### 技能骰子公式格式

`{diceCount}D{diceFaces}` 形式（如 `2D6`），`SkillData.prepareDerivedData()` 自动从等级/EGO等级派生，可在物品卡手动覆盖。

### 星芒/经验相关常量（`module/config.mjs`）

```js
// 普通技能星芒消耗
SKILL_COSTS: { 1: 1, 2: 5, 3: 10 }
// EGO技能星芒消耗（按等级）
EGO_COSTS: { ZAYIN: 1, TET: 3, HE: 5, WAW: 10, ALEPH: 15 }
// 升级所需经验：LEVEL_XP 表（Lv1-50），超过50级后每5级 ×1.5 缩放（getXpForLevel）
```

`config.mjs` 还挂载了：七宗罪标签/图标/颜色、技能分类图标、物理伤害类型、六属性标签、抗性倍率选项（x0.5~x3.0）、BUFF 分类目录（含自定义注册的与通用的）、回合结束自动清除的 BUFF 集合、技能/装备子类型枚举、Activity 触发时机(16种)与效果类型(10种)、骰子类型(normal/unbreakable/severing)。

---

## 七、已知空白与后续方向

代码库里没有任何 TODO/FIXME 标记——以下是基于当前实现状态观察到的、尚未完整落地的点，供后续规划参考，而非已排期的任务列表：

- **特殊骰类型（不可摧毁/截断骰）**：`config.mjs` 的 `DICE_TYPES` 已定义 `unbreakable`/`severing`，`clash.mjs` 也有 `_triggerUnbreakableCounter` 等相关逻辑，但对应图标资源标注为"暂未启用"，UI 层是否完整覆盖两种特殊骰的全部规则尚待逐条核对策划文件确认。
- **`templates/combat/combat-hud.hbs`**：已注册预加载，但没有找到对应的独立 Application 类在使用它，可能是遗留脚手架或由 `clash.mjs` 内联拼装聊天卡片时另行处理——如果不再需要，可以考虑清理；如果计划做成独立战斗 GUI，需要先确认设计意图。
- **`template.json` 与 `system.json` 不同步**：前者的 Item 类型列表缺少 `panic`/`background`，建议后续要么同步更新，要么彻底移除以避免误导（其 `_comment` 已注明是兼容占位，非权威来源）。
- **背景系统缺少独立策划文档**：目前规格完全以代码实现（`item.mjs` 的 `BackgroundData`、`background-wizard.mjs`、`level-up-dialog.mjs`）为准，没有对应的 `策划文件/背景系统.md`。如果后续要交给非开发人员维护规则，建议补一份。
- **DLC / 扩展玩法**（策划文件中已有构想但未实现，规模较大，需要先补策划细节再排期）：
  - 心（Heart）系统：消耗情感记忆激活金光强化
  - 望（Aspiration）系统：心能量转化为1-5层光环加成
  - 恐慌系统的进一步交互扩展

> **开发提示**：遇到任何规则疑问，先查 `策划文件/` 对应文档；代码架构、常用实现模式与开发流程参照 `CLAUDE.md`。GUI 界面制作时，配合 `assets/icons/GUI/` 中的截图作为像素级参考。
