# Foundry VTT v14 升级手册

> 这份文档是给**专门做 v14 升级的那个会话**看的。开工前先把 `CLAUDE.md` 读完（架构约定都在那儿），
> 这里只写「升到 v14 要动什么、为什么、怎么验」。
> 升级期间的所有改动请**只**做兼容性改造，不要顺手重构业务逻辑——出问题时要能一眼分清是升级砸的还是重构砸的。

## 0. 升级前必读的三件事

1. **v14 不能原地升级**：必须干净安装 v14，不要覆盖 v13 的安装目录。
2. **世界是单向的**：一个 world 用 v14 打开过之后，**再也回不到 v13**。
   动手前先完整备份 `Data/worlds/<你的世界>/` 和 `Data/systems/limbusCompany_FVTT/`。
3. **回退路径**：代码这边的回退就是切回升级分支的父提交；世界那边的回退**只能靠上面那份备份**。
   所以顺序永远是：备份世界 → 改代码 → 复制一份世界出来当小白鼠 → 在小白鼠世界里验 → 才动正式世界。

## 1. 结论先行：这次升级不大

v14 **没有**删掉 ApplicationV1。`Application` / `Dialog` / `FormApplication` / `DocumentSheet`
仍然只是 deprecated，删除排期在 **v16**。本系统的 13 个 V1 类：

- `LimbusActorSheet` / `LimbusMerchantSheet` / `LimbusCampSheet` / `LimbusLootSheet`（`extends ActorSheet`）
- `LimbusItemSheet`（`extends ItemSheet`）
- `SinResourceHUD` / `SquadHUD` / `QuickActionHUD` / `GMConsole` / `BackgroundWizard` /
  `LevelUpDialog` / `CSVImportDialog`（`extends Application`）

在 v14 下**照常工作**，只是控制台刷弃用警告。
**不要为了上 v14 去把它们重写成 ApplicationV2**——那是独立的、远更大的一次改造，
留给 v15 时间窗口做，且必须一个 sheet 一个 PR。

v14 真正**移除**的东西，本仓库全部没用到（已 grep 确认，可复查）：

| v14 移除项 | 本仓库状态 |
|---|---|
| `MeasuredTemplate` 文档类型（整个删除） | 无引用 |
| TinyMCE（ProseMirror 成为唯一编辑器） | 所有 `{{editor}}` 已显式 `engine="prosemirror"` |
| `ApplicationV2#bringToTop` | 无引用 |
| `colorPicker` handlebars helper | 无引用 |
| v12 期弃用的 `foundry.utils.*` 裸全局（`mergeObject` / `duplicate` / `setProperty` …） | 已全部走 `foundry.utils.` 命名空间 |

## 2. 必改项（按风险从高到低）

改法统一用**兼容双写**：新 API 优先、旧 API 兜底，写成 `新 ?? 旧`。
这样同一份代码 v13 和 v14 都能跑，可以先在 v13 上验证没砸，再去装 v14。
仓库里已有现成范式，照抄 `module/helpers/asset-lookup.mjs:53-56`：

```js
/** v13 把 FilePicker 挪进了 foundry.applications.apps，老版本还在全局 */
return foundry?.applications?.apps?.FilePicker?.implementation ?? globalThis.FilePicker;
```

### ① `renderChatMessage` 钩子 —— 最高风险

**位置**：`module/limbusCompany_FVTT.mjs:438`（唯一一处，但它是整个战斗流程的总闸）

**问题**：v13 起 `renderChatMessage` 被 `renderChatMessageHTML` 取代。
新钩子第二个参数是**原生 `HTMLElement`，不再是 jQuery 对象**。

**为什么是最高风险**：这个钩子里挂着「▼ 详细信息」折叠，以及三个结算处理器
`clash-resolve` / `clash-counter` / `clash-block`（见 `CLAUDE.md` 的「延迟结算」一节——
流血、追加伤害、发起对抗、反应检查、恐慌鉴定**全部**在【结算结果】按钮上落地）。
钩子一旦不触发，**所有战斗卡片的按钮集体变死键，而且不抛错、不报警、完全静默**。
自动化测不出来，只能手点。

**改法**：注册新钩子名，入口处把原生元素包一层 jQuery，函数体一行不用动：

```js
const _onRenderChatMessage = (_message, html, _data) => {
  const $html = html instanceof jQuery ? html : $(html);   // v14 给的是 HTMLElement
  // …… 以下原样保留，继续用 $html.find(...)
};
Hooks.on("renderChatMessageHTML", _onRenderChatMessage);   // v13+
Hooks.on("renderChatMessage",     _onRenderChatMessage);   // v12 兜底，可择机删
```

注意别两个钩子在同一版本里都触发导致**监听器绑两遍**（按钮点一次跑两次 = 伤害算两次）。
稳妥写法是只注册存在的那个，或者在处理器里用 `html.dataset.limbusBound` 之类的标记去重。

**验收**：见 §4 的战斗清单，这一项必须逐条手点。

### ② `TextEditor` 裸全局

**位置**：`module/sheets/actor-sheet.mjs:1032`，`TextEditor.getDragEventData(event)`

**问题**：已迁到 `foundry.applications.ux.TextEditor.implementation`。

**影响**：这是角色卡**拖拽落点**的入口——装备槽、技能槽、6 格背包、背景卡全靠它解析拖拽数据。
断了就是「拖进去没反应」。

**改法**：
```js
const TE = foundry.applications?.ux?.TextEditor?.implementation ?? globalThis.TextEditor;
const data = TE.getDragEventData(event);
```

### ③ `loadTemplates` 裸全局

**位置**：`module/limbusCompany_FVTT.mjs:1302`（`_preloadTemplates()` 的返回语句）

**改法**：`(foundry.applications?.handlebars?.loadTemplates ?? globalThis.loadTemplates)(templatePaths)`

**影响**：所有 partial 渲染不出来。这条挂了**一眼就能看见**，风险低。
顺带提醒：新增 `.hbs` 必须进这个数组，规矩没变。

### ④ `Actors` / `Items` 全局集合

**位置**：`module/limbusCompany_FVTT.mjs:150 / 156 / 162 / 168 / 175`（5 处 `registerSheet`）

**改法**：`const Actors = foundry.documents?.collections?.Actors ?? globalThis.Actors;`（Items 同理），
然后 5 处调用不动。

**影响**：sheet 注册不上 → 所有文档回落到 Foundry 默认表单。同样是一眼可见。

### ⑤ `system.json` 的 compatibility

```json
"compatibility": { "minimum": "13", "verified": "14" }
```
**`verified` 等真跑通 §4 全部清单之后再写**，不要一上来就填。
`minimum` 保持 13——上面全是兼容双写，没有理由断掉 v13 用户。
改完记得 `python3 -c "import json; json.load(open('system.json'))"`。

## 3. 可选项（**这次别做**，登记在案供以后排期）

- **`new Dialog(...)` ×19 / `Dialog.confirm(...)` ×16**，分布：
  `clash.mjs` 5、`item-sheet.mjs` 6、`loot-sheet.mjs` 7、`actor-sheet.mjs` 5、`camp-sheet.mjs` 3、
  `merchant-sheet.mjs` 3、`limbusCompany_FVTT.mjs` 2、`dice.mjs` 1、`recipe-editor.mjs` 1、
  `csv-import-dialog.mjs` 1、`quick-action-hud.mjs` 1。
  v14 下全部可用。将来迁 `DialogV2` 时，先包一个 `limbusConfirm()` 薄封装，把 35 处收敛成一处再换实现。
- **ApplicationV2 迁移**：见 §1，留给 v15。

## 4. 验收清单（改完必须逐条手点）

代码层面先过一遍静态检查：

```bash
for f in $(git diff --name-only | grep '\.mjs$'); do node --check "$f" || echo "FAIL $f"; done
python3 -c "import json; json.load(open('system.json'))"
```

然后**在 v14 里开小白鼠世界**手点。带 ★ 的是本次改动的直接暴露面，一条都不能跳：

**渲染 / 注册**
- [ ] 系统能正常载入，控制台无 `Error`（deprecation `Warning` 可以有）★③★④
- [ ] 角色卡 / 五种物品卡 / 商人 / 营地 / 战利品卡都能打开，且是本系统的卡不是 Foundry 默认表单 ★④
- [ ] 物品卡的 `{{editor}}` 富文本框能编辑并保存（ProseMirror）

**战斗流程 —— 全靠 §2① 的钩子**
- [ ] 发起一次拼点对抗 → 对抗卡出现 → 点【对抗】能弹出响应对话框 ★①
- [ ] 点【承受】能直接结算 ★①
- [ ] 点【结算结果】：HP 变化正确，且顺序是 流血 → 主伤害 → 不可摧毁反击 → 追加伤害 → 承受聚合 → 恐慌鉴定 → 发起对抗 → 反应检查 ★①
- [ ] **按钮点一次只结算一次**（验钩子有没有绑两遍）★①
- [ ] 「▼ 详细信息」折叠能展开收起——**包括先攻骰掷那张纯 content、无 flags 的卡** ★①
- [ ] 反击卡 / 格挡卡的结算按钮同样有效 ★①
- [ ] 闪避成功 / 完全格挡 / 0 伤害格挡这三条**没有结算按钮**的分支，流血和追加伤害仍即时publish
- [ ] 容量扩散承受卡的按钮有效 ★①

**拖拽 —— 全靠 §2② **
- [ ] 往装备槽拖装备 ★②
- [ ] 往技能槽拖技能 ★②
- [ ] 6 格背包内拖拽排序 ★②
- [ ] 往角色卡拖背景卡 ★②

**其它**
- [ ] 右键上下文菜单（装备槽 / 技能槽 / 背景）
- [ ] 角色创建向导走完整流程；升级对话框三步走完
- [ ] CSV 导入一次（同名合并、图片按名匹配）
- [ ] Token 生命环 / 理智圆 / BUFF 图标行显示正常，且**不会盖住相邻 token**
      —— v14 的 Scene Levels 改动可能影响 `canvas.primary` 的 elevation/sort 语义，
      这条代码扫不出来，**只能靠眼睛**，见 `CLAUDE.md` 的 TokenRingHUD 一节

## 5. 分支与提交约定

- 在**新分支**上做，从当前默认分支切出，别和别的功能混。
- 建议拆两个提交：`①②③④` 的兼容双写一个提交，`system.json` 的 `verified: "14"` 单独一个提交
  ——这样万一 v14 验不过，回退掉后面那个就行，兼容改造本身对 v13 是无害的，可以留着。
- 出大问题的回退：`git revert` 或直接切回父提交；**世界的回退只能靠 §0 的备份**。

## 6. 参考

- [API Migration Guides](https://foundryvtt.com/article/migration/)
- [Release 14.365](https://foundryvtt.com/releases/14.365)
- [ApplicationV2 Conversion Guide](https://foundryvtt.wiki/en/development/guides/applicationV2-conversion-guide)
